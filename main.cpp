#include <iostream>

using namespace std;

int main() {
    /*
    Population pop = PopulationCreator.create();

    EvolvingProces proc(pop, selekcja, breeding);
    proc.evolve();

    // to będzie w EvolvingProcess
    while ( warunekZakończeniaEwolucji ) {
        // selekcja eliminuje osobników i zwraca najmocniejsze ogniwa
        ranking = SelectionStrategy.rank(pop);
        // tutaj zapiszemy do historii najlepszy fenotyp
        SelectionStrategy.eliminate(pop, ranking);
        BreedingStrategy.breed(pop);
    }
     */

    return 0;
}